
function Slideshow(images) {
    return (
        <img  className="logement__slideshow" src={images} alt="logement"/>
    )
};

export default Slideshow


            //{images.map((image, index) => (
               // <img key={index} src={image} alt="logement"/>
            //))}